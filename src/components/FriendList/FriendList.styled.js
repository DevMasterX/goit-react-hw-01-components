import styled from 'styled-components';

export const FriendListWrap = styled('ul')(() => {
  return {
    width: '400px',
    margin: 'auto',
    marginTop: '20px',
    marginBottom: '40px',
    paddingLeft: '0',
  };
});
